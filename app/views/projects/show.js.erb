console.log("Project - Show (project properties)");

with($.booka) {
  layout.place(<%= js_str @project.title %>);
  layout.requestBrowser('projects', '<%= browser_projects_path %>');
  layout.document(<%= js_partial @project %>);
}
