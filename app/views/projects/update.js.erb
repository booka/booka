
with($.booka) {
   layout.browser('projects', <%= js_partial 'projects/browser' %>);
   dialogs.close();
   layout.flash(<%= js_str "La investigación '#{h @project.title}' ha sido actualizada." %>);
}
