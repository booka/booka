console.log("ProjectIndexes - show");

<%= js_project @project %>
with($.booka) {
  layout.browser('index', <%= js_partial 'indexes/browser' %>);
  layout.flash(<%= js_str "Índice de '#{@project.title}' cargado." %>);
}
