console.log("Activities - Index (activity page)");

<%= js_before %>
with($.booka) {
  layout.place(<%= js_str @activity_report.title %>);
  layout.browser(null);
  layout.document(<%= js_partial @activity_report %>);
  layout.flash(<%= js_str "Estás viendo la #{@activity_report.title}" %>);
}