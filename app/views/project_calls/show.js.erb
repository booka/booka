console.log("ProjectCalls - Show (call document)");

<%= js_before %>
with($.booka) {
  layout.place(<%= js_str "Convocatoria de '#{@project.title}'" %>);
  layout.requestBrowser('projects', <%= js_str browser_projects_path %>);
  layout.document(<%= js_partial @call %>);
  layout.preDocument(<%= js_partial 'projects/stages' %>);
  comm.add('document', <%= js_str "Estás leyendo la convocatoria de '#{@project.title}'" %>);
}

// js_tooltip_for 'navigate-to-archives' do
//  La investigación '<%= h @project.title %>' está en fase de presentación de propuestas. Pulsa aquí para verlas.
// end 

