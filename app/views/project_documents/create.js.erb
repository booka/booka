console.log("ProjectDocuments - create");

with($.booka) {
  // reload the browser to reflect the changes
  layout.browser('documents', <%= js_partial '/documents/browser' %>);
  dialogs.close();
  layout.flash(<%= js_str "Documento '#{@document.title}' añadido al archivo." %>);
}
