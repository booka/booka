<% if has_project? %>
  <%= navigate_to :entrance, projects_path %>
  <%= navigate_to :archives, project_documents_path(@project)  %>
  <%= navigate_to :edition, project_disqs_path(@project)  %>
  <%= navigate_to :booka, project_index_path(@project)  %>
<% else %>
  <%= navigate_to :entrance, projects_path %>
<% end %>
