<%= navigate_to true, :entrance, 'Investigaciones', projects_path %>
<% if has_project? %>
  <%= navigate_to @project.stage?(:one), :archives, 'Archivos', project_documents_path(@project) %>
  <%= navigate_to @project.stage?(:two), :edition, 'Discusiones', project_disqs_path(@project) %>
  <%= navigate_to @project.stage?(:three), :booka, 'Índice', project_index_path(@project) %>
<% end %>

