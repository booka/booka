
console.log("Chapters - Show");

<%= js_project @project %>
with($.booka) {
  layout.place(<%= js_str @document.title %>);
  layout.requestBrowser('index', '<%= project_index_path(@project) %>');
  layout.document(<%= js_partial @document %>);
  layout.flash(<%= js_str "'#{@document.title}' cargado." %>);
}
