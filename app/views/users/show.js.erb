
console.log("Users - Show");

with($.booka) {
  layout.place(<%= js_str @user.name %>);
  layout.requestBrowser('users', '<%= users_path %>');
  layout.document(<%= js_partial @user %>);
  layout.flash(<%= js_str "'#{@user.name}' cargado." %>);
}
